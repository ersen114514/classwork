average = [0]
while (num := float(input("your number"))) >= 0:
    average.append(num)
print(f"average number is {sum(average) / max(len(average),1)}" if max(len(average),1) != 0 else "no average number")
